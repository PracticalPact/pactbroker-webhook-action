name: "gateway-register-participants"
description: "Registers repository URL for all gateway-related participants in the Pact Broker"
inputs:
  brokerUrl:
    description: "Pact Broker URL"
    required: true
  gatewayName:
    description: "Gateway application name"
    required: true
runs:
  using: "node20"
  main: "index.js"